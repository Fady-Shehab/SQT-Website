name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - staging
      - dev
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name }}
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - run: yarn install --frozen-lockfile

      - run: yarn build

      - name: Prepare deployment
        run: |
          # SPA fallback — GH Pages serves 404.html for unknown paths, which boots the React app
          cp dist/index.html dist/404.html

          # Copy root JS files (overwrites any empty placeholders from public/js)
          cp -rf js/* dist/js/ 2>/dev/null || true

          # Ensure .nojekyll exists (GitHub Pages ignores _underscore_ paths without it)
          touch dist/.nojekyll

      - uses: actions/configure-pages@v4

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
